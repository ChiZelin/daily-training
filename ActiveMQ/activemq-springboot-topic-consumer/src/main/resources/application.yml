server:
  port: 5555

spring:
  activemq:
    broker-url: tcp://127.0.0.1:61616
    user: admin
    password: admin

  jms:
    pub-sub-domain: true # false = Queue true = Topic

myTopic: boot-activemq-topic